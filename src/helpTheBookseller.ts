export class G964 {
    public static stockList = (listOfArt:string[], listOfCat:string[]) => {
        if (listOfArt.length === 0 || listOfCat.length === 0) {
            return ""
        } else {
            
        }

    }
}