export function multiplicationTable(size: number): number[][] {
    const iArray = [];
    const jArray = [];
    for (let i = 0; i < size; i++) {
        jArray.length = 0;
        for (let j = 0; j < size; j++) {}
    }
    return [[0]];
}
